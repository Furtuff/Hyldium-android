<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="button_add" parent="Widget.Design.FloatingActionButton">
        <item name="android:src">@android:drawable/ic_menu_add</item>
        <item name="backgroundTint">@android:color/holo_blue_bright</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="button_edit" parent="Widget.Design.FloatingActionButton">
        <item name="android:src">@android:drawable/ic_menu_edit</item>
        <item name="backgroundTint">@android:color/holo_blue_bright</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="button_delete" parent="Widget.Design.FloatingActionButton">
        <item name="android:src">@android:drawable/ic_menu_delete</item>
        <item name="backgroundTint">@android:color/holo_red_light</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>

    <style name="button_order" parent="Base.Widget.AppCompat.ImageButton">
        <item name="android:src">@android:drawable/ic_media_play</item>
        <item name="backgroundTint">@android:color/holo_green_light</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
    </style>
</resources>
